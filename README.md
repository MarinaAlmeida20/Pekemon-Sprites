# Pekemon Sprites Demo

In this project was applied the DOM properties and Methods and manipulating the HTML with:

Loop - The loop was used to count and pass on throw it element.

classList - Used to create the new class.

appendChild() - used to display the img and label.

createElement - used to create an element that no exist in the HTML

innerHTML - used to display the numbers below the img.


 
